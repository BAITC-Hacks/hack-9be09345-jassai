# Проверки части C

Дата: 2026-09-23. Windows, Node.js v24.21.0, Python 3.13.15 (только локальный предпросмотр).

## Выполнено

- `node tests/frontend.test.mjs`: 12/12 passed. XSS-экранирование карточек/ошибок, три фактора, маркировка источника, отсутствие API-секретов в HTML, multipart, CSRF и идемпотентный ключ, timeout, не-JSON ответ, read-only HR, демонстрационный переход завершения.
- `node --check web/app.js`: успешно.
- Browser: предпросмотр сотрудника; начать → выполнить → изменение уровня и покрытия → обновлённые карточки; проверено.
- Визуально: узкая ширина около 420 px и desktop 1440 px; основные блоки читаемы, адаптивная сетка переключается.

`node --test` в данном окружении не смог создать дочерний процесс (EPERM); прямой запуск файла Node test runner выполнил те же 12 тестов успешно.

## Пока не проверено

Реальные backend-сессии и права; транзакции/идемпотентность сервера; импорт полного набора; точность расчётов; OpenAI/NVIDIA с ключами; задержки реальных рекомендаций; launcher на чистой Windows; сохранение после перезапуска. Предпросмотр не является доказательством этих свойств. Эти проверки выполняются после подключения частей A/B.
