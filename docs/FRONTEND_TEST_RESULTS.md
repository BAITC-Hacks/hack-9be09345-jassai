# Проверки части C

Дата: 2026-09-23. Windows, Node.js v24.21.0, Python 3.13.15. Backend A: commit 470aebb.

## Автоматические проверки

- `node tests/frontend.test.mjs`: **17/17 passed**. Экранирование импортированного текста; минимум три фактора; маркировка источника; отсутствие API-секретов в HTML; multipart; CSRF/idempotency; timeout; не-JSON ответ; read-only HR; адаптация профиля, recommendations[].event, HR-знаменателей, setup/login и event_id/record_id к реальному контракту A.
- `node --check frontend/app.js`, `node --check frontend/views.js`, `node --check frontend/backend.js`: успешно.
- Тесты A `python -m pytest -q`: **38 passed**, 1 предупреждение Starlette о deprecated httpx в TestClient. Серверные файлы A не менялись.
- Проверка использовала закреплённые верхнеуровневые зависимости pyproject.toml, установленные в отдельную локальную папку .test-deps. Это не проверка чистой установки uv.lock/launcher и не проверка Python 3.12.14.

`node --test` в sandbox не смог создать дочерний процесс (EPERM); прямой запуск файла Node test runner выполнил те же тесты успешно.

## Браузер + настоящий FastAPI/SQLite

Запущен A на 127.0.0.1:8766 с отдельной синтетической тестовой БД вне публикуемых файлов. URL без preview.

| Проверка | Результат |
|---|---|
| Вход/выход сотрудника и HR | Успешно; cookie/CSRF работают через браузер |
| Профиль, цель, история, имена навыков | Отображаются из реального API A |
| Начать EV_GOOD через каталог допустимых шагов | Создана запись участия, кнопка сменилась на выполнение |
| Выполнить запись | Навык Architecture 2 → 4, покрытие 40% → 80%, история completed |
| HR-обзор | Три таблицы отражают серверные агрегаты |
| Импорт ранее неизвестного NEW_UI_JURY | Файлы загружены браузером; validate valid=true; apply добавил один профиль |
| Профили и доступ после импорта | Новый профиль появился в таблице и списке выбора аккаунта |
| Неподключённый AI | Показано явное сообщение; нет фиктивной рекомендации |
| Отсутствующие маршруты настроек B | Формы видны, ввод ключей отключён |

Создание аккаунта проверено тестами сервера/адаптером; сам submit формы создания нового пароля не проходился в браузере. Первичный bootstrap проверен тестами A и маршрутизацией адаптера; браузерный мастер на чистой БД ещё не пройден.

## Предпросмотр и визуальная проверка

Синтетический режим отдельно: начать → выполнить → before/after → обновлённые карточки. Проверены узкая ширина около 420 px и desktop 1440 px. Данные организаторов не включены в frontend fixtures.

## Осталось команде

- Подключить B: реальные OpenAI/NVIDIA, модель, ключ, проверка фактов, режимы отказа и кэш провайдера.
- Проверить сохранение ключей DPAPI/Credential Manager и реальный AI-тайм-аут.
- Замерить реальные AI-задержки, качество на проверочных профилях и UI на полном наборе.
- Пройти launcher/мастер на чистой Windows и перезапуск приложения.
- Пройти полный сценарий нового профиля с настоящей рекомендацией и финальной версией всех частей.

Результаты синтетического предпросмотра не доказывают качество LLM и работоспособность launcher.
